_: {
  # users.users.${user} = {
  #   name = "${user}";
  #   home = "/Users/${user}";
  #   isHidden = false;
  #   shell = pkgs.zsh;
  # };

  # home.packages = with pkgs; [
  #   nvim
  # ];

  # I think this will cache everything
  # perhaps --offline, or --option substitute false --option binary-caches ""
  # system.includeBuildDependencies = true;
}
